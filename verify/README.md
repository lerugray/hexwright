# verify/

Headless smoke test (Playwright). Run:
```
npm i playwright && npx playwright install chromium
node smoke.mjs
```
Screenshots 01-loaded / 02-inspector / 03-assigned are the last verified run (11/11 checks).
